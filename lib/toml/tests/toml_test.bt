// SPDX-FileCopyrightText: 2024-present Lukas Neubert <lukas.neubert@proton.me>
// SPDX-License-Identifier: MPL-2.0

import os
import toml

fun test_data() {
	text := os.read_file(os.join_path(os.dir($FILE), ['data', 'sample.toml']))
	toml_doc := toml.parse_text(text)!
	// ...
}
